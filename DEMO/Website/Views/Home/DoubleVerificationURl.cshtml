
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>DoubleVerificationURl </title>
</head>

<body>
    @if (TempData["responseString"] != null)
    {
        <div>@TempData["responseString"]</div><br /><br />
    }
     College Admission
    <div> 
        @using (Html.BeginForm("DoubleVerificationURl", "Home", FormMethod.Post, new { id = "myform", enctype = "multipart/form-data" }))
        {
            <br />
            @Html.TextBox("trxid", "", new { @class = "form-control",  @required = "required", @placeholder = "Orderno" })

            <br />            <br />
            @Html.TextBox("password", "", new { @class = "form-control", @required = "required" , @placeholder = "password" })

            <br />            <br />
            @Html.TextBox("mid", "", new { @class = "form-control", @required = "required" ,@placeholder="Mid"})

            <br />            <br />
            <input type="submit" value="submit"/>

        }

    </div>
    
    <div>
        @using (Html.BeginForm("DoubleVerificationURlsee", "Home", FormMethod.Post, new { id = "myform", enctype = "multipart/form-data" }))
        {
            <br />
            @Html.TextBox("trxid", "", new { @class = "form-control", @required = "required", @placeholder = "Orderno" })

            <br />            <br />
            @Html.TextBox("password", "", new { @class = "form-control", @required = "required", @placeholder = "password" })

            <br />            <br />
            @Html.TextBox("mid", "", new { @class = "form-control", @required = "required", @placeholder = "Mid" })

            <br />            <br />
            <input type="submit" value="Only See Status" />

        }

    </div>
</body>
</html>
